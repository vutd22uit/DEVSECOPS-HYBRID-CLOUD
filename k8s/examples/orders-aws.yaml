# ================================================================= #
# Kubernetes Deployment Manifest for Orders Service
# Cloud Provider: AWS EKS
# ================================================================= #

apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-service
  namespace: foodhub
  labels:
    app: orders
    cloud: aws
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: orders
  template:
    metadata:
      labels:
        app: orders
        cloud: aws
        tier: backend
    spec:
      containers:
      - name: orders
        image: 257394468168.dkr.ecr.ap-southeast-1.amazonaws.com/foodhub-orders:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8084
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "production"
        - name: DB_HOST
          value: "orders-db.foodhub.svc.cluster.local"
        envFrom:
        - secretRef:
            name: orders-db-credentials
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8084
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: foodhub
  labels:
    app: orders
    cloud: aws
spec:
  type: ClusterIP
  ports:
  - port: 8084
    targetPort: 8084
    protocol: TCP
    name: http
  selector:
    app: orders
